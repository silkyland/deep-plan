{
  "name": "already-done",
  "version": "1.0.0",
  "private": true,
  "description": "The requested feature already exists and is tested — deep-plan eval fixture for the zero-gaps path",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test"
  }
}
