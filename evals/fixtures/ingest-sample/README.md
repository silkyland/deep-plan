# ingest-sample

A `deep-plan-ingest` eval fixture: a finished `docs/PLAN.md` carrying all
three tag types (VERIFIED, UNVERIFIED, CORRECTED) so the ingest can be
checked for correct filtering. Expected behavior is in `../../scenarios.md`
(Scenario 4).
