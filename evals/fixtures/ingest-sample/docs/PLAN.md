# Plan: Add discount-cap enforcement to the cart

> Generated by deep-plan on 2026-01-10. Ground-truth source: vendor/rule-engine (local).

## 1. Context

Enforce the configured discount cap in cart pricing. Done when no line can
exceed `discountCapPercent`. Non-goal: redesigning the pricing UI.

Research questions:
1. Where is the cap defined? — VERIFIED `src/cart/config.js:9`
2. Does the rule engine expose an apply hook? — VERIFIED `vendor/rule-engine/RuleInterface.js:16`
3. What is real prod cart size? — UNVERIFIED: no access to prod metrics

## 2. Findings: current state

| Component | Status | Evidence |
|-----------|--------|----------|
| `src/cart/PriceCalculator.js` | works | called by cart total path |
| `discountCapPercent` setting | half-wired | collected `src/admin/Settings.vue:14`, never read |

## 3. Findings: framework ground truth

| Mechanism | Fact | Source |
|-----------|------|--------|
| Rule engine | rules implement `match`/`apply`, `apply` mutates scope in place | `vendor/rule-engine/RuleInterface.js:16` |

## 4. Audit findings

- **[correctness] Discount cap not enforced** — `applyDiscount` has no upper
  bound (`src/cart/PriceCalculator.js:12`).

## 5. Corrections

- CORRECTED: initially believed the cap was enforced in the API layer; it is
  not enforced anywhere (`src/cart/PriceCalculator.js:12`). The old belief
  must NOT be ingested — only the correction.

## 6. Gap analysis

1. BROKEN — cap defined but never enforced (§4).
2. MISSING — no rule wired for the cap (§2, §3).

## 7. Design decisions

### Decision 1: enforce the cap via a rule-engine rule
- **Decision:** add a `DiscountCapRule` implementing `RuleInterface`.
- **Reversibility:** REVERSIBLE.
- **Rationale:** the framework blesses rules for this (`vendor/rule-engine/RuleInterface.js:16`).

## 8. Phased roadmap

### Phase 0: correctness fix  (effort: S)
- [ ] Clamp `discountPercent` to the cap in `src/cart/PriceCalculator.js`.

### Phase 1: rule-engine wiring  (effort: M)
- [ ] Spike: confirm real prod cart size before assuming O(n) apply is fine (RQ 3).
- [ ] Add `DiscountCapRule` in `src/cart/rules/DiscountCapRule.js`.

## 9. Risk register

| Risk | Likelihood | Impact | Mitigation / rollback |
|------|-----------|--------|----------------------|
| Cap change alters historical order totals | medium | high | apply only to new carts; feature-flag |

## 10. Verification

    node --test

Manual: create a line with 90% discount, confirm it clamps to 50%.
